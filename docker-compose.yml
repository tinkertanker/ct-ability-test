services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - db_data:/app/backend/data
    environment:
      NODE_ENV: production
      PORT: 3000
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set}

volumes:
  db_data:
